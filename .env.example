# Required. Keep this server-side; never expose it to the browser.
OPENAI_API_KEY=sk-...

# Optional model and server configuration.
OPENAI_MODEL=gpt-5.5
PORT=3001
SESSION_STORE_PATH=.data/sessions.json
